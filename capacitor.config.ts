import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cripto.p2',
  appName: 'cripto-p2-frontend',
  webDir: 'dist/cripto_p2_frontend/browser'
};

export default config;
