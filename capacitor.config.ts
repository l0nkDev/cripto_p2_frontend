import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cripto_p2_frontend',
  webDir: 'dist/cripto_p2_frontend/browser',
};

export default config;
